import { getTokenData } from "@/utils";
import { useGetDatasetQuery } from "../../api/api";

const Home = () => {
  // const token = getTokenData() as { _id: string };
  // console.log(token);
  // const { data } = useGetDatasetQuery(token._id);
  // console.log(data);
  return <div>
    
  </div>;
};

export default Home;
